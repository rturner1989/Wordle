export enum keyState {
    DEFAULT = "default",
    INCORRECT = "incorrect",
    CORRECT = "correct",
    WRONGLOCATION = "wrongLocation",
}

export enum gameDifficulty {
    EASY = "easy",
    NORMAL = "normal",
    HARD = "hard",
}
